{
  "name": "stellarjs-test",
  "version": "0.0.0",
  "dependencies": {
    "@ledgerhq/hw-transport-node-hid": "^3.0.0",
    "@ledgerhq/hw-transport-u2f": "^3.0.0",
    "babel-runtime": "^6.26.0"
  },
  "devDependencies": {
    "stellar-sdk": "^0.7.3",
    "browserify": "^13.1.0",
    "uglify-js": "2.7.3"
  },
  "scripts": {
    "browserify": "mkdir -p browser && browserify --standalone StellarLedger --outfile browser/stellar-ledger-api.js src/browser.js",
    "browserify-test": "mkdir -p browser && browserify --standalone runScript --exclude stellar-sdk --outfile browser/test.js test/browser.js",
    "uglify": "uglifyjs -o browser/stellar-ledger-api.min.js browser/stellar-ledger-api.js",
    "clean": "rm -rf browser"
  }
}
